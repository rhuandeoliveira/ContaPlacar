import { StyleSheet } from 'react-native'

export default StyleSheet.create({
    teamText: {
        fontSize: 30,
        fontWeight: 'bold',
      },
    pointsText: {
        fontSize: 100,
        marginVertical: 20,
        fontWeight: 'bold'
      },
})